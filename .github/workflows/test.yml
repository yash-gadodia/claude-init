name: test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  self-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run claude-init self-tests
        run: bash tests/run.sh
